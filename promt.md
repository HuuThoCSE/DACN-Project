```

Vai trò là sinh viên đang viết báo cáo đồ án chuyên ngành khoa học máy tính, 

Về chủ đề “Phát hiện và phân đoạn khối u trên nhũ ảnh sử dụng thuật toán K-mean và Fuzzy C-mean trong môi trường xử lý dữ liệu lớn”. 

Sử dụng công nghệ: 
- Hadoop làm hdfs lưu trữ dữ liệu; 
- Dùng spark với công nghệ spark-submit để xử lỷ ảnh. Spark chạy dưới chế độ client trả về cho master spark, spark chạy ứng dụng Python, vì chạy ứng dụng python nên không thể chạy spark cluster 

Nguyên lý: Lấy ảnh từ hdfs xử lý ảnh với thuật toán xử lý ảnh sau đó lưu ảnh lại hdfs

Tập trung vào phần big data, xử lý dữ liệu lớn.

```

```

Vai trò là sinh viên đang chuẩn bị báo cáo đồ án chuyên ngành khoa học máy tính, 

Về chủ đề “Phát hiện và phân đoạn khối u trên nhũ ảnh sử dụng thuật toán K-mean và Fuzzy C-mean trong môi trường xử lý dữ liệu lớn”. 

Tập trung vào phần big data, xử lý dữ liệu lớn.

Sử dụng công nghệ: 
- Hadoop làm hdfs lưu trữ dữ liệu; 
- Dùng spark với công nghệ spark-submit để xử lỷ ảnh. Spark chạy dưới chế độ client trả về cho master spark, spark chạy ứng dụng Python, vì chạy ứng dụng python nên không thể chạy spark cluster 

Nguyên lý: Lấy ảnh từ hdfs xử lý ảnh với thuật toán xử lý ảnh sau đó lưu ảnh lại hdfs

Hãy cho tôi 1 kịch bản thuyết trình về bài báo cáo này trước giảng viên, chuyên nghiệp, gấy ấn tượng, khoa học, logic

```